import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.IonicAngularCourse',
  appName: 'ionic-angular-course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
